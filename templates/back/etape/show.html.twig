{% extends 'base.html.twig' %}

{% block title %}Etape{% endblock %}

{% block body %}
    <h1>Etape</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etape.id }}</td>
            </tr>
            <tr>
                <th>NumeroEtape</th>
                <td>{{ etape.numeroEtape }}</td>
            </tr>
            <tr>
                <th>VilleEtape</th>
                <td>{{ etape.villeEtape }}</td>
            </tr>
            <tr>
                <th>NombreJours</th>
                <td>{{ etape.nombreJours }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back/etape_index') }}">back to list</a>

    <a href="{{ path('back/etape_edit', {'id': etape.id}) }}">edit</a>

    {{ include('back/etape/_delete_form.html.twig') }}
{% endblock %}